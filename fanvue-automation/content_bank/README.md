Drop jpg, png, or webp teasers here. ContentAgent uploads new files
(max 20 per run) and posts up to 5 follower-visible teasers. Running
twice will not re-upload the same file.

Only files in this folder are uploaded. Empty-room stills belong in
`../datasets/rooms/` so they are not posted as teasers.

Starter bank (local, gitignored): identity source + 12 SFW portraits
from the reference photo. Public captions stay lifestyle/teaser, not PPV.
